        <form method="POST">

        <div>
            <label for="first">First Name</label>
            <input type="text" id="first" name="first" label="first" value="{{user_profile.users_first}}" autofocus>
        </div>
        
        <div>
            <label for="last">Last Name</label>
            <input type="text" id="last" name="last" label="last" value="{{user_profile.users_last}}" >
        </div>

        <div>
            <label for="email">Email Address</label>
            <input type="text" id="email" label="email" name="email"  value="{{user_profile.users_email}}" >
        </div>

        <div>
            <label for="password">Password</label>
            <input type="text" id="password" label="password"  name="password">
        
        </div>

            <div>
                <label for="age">Age</label>
                <input type="text" id="age" label="age" name="age"  value="{{user_profile.users_age}}" >
            
            </div>

            <div>
                <label for="city">City of residence</label>
                <input type="text" id="city" label="city" name="city"  value="{{user_profile.users_city}}" >
            </div>

            <div>
                <label for="age">Your homepage's url</label>
                <input type="text" id="url" label="url" name="url"  value="{{user_profile.users_url}}" >
            
            </div>
            <input type="hidden" name="_csrf" value="{{csrfToken}}">
            <div>
                <input type="submit" value="update my profile" id="submit">
            
            </div>
                </form>
        </div>
            {{#if error}}
        <div class="error-text">Oh no! Something went wrong! Please try again!</div>
            {{/if}}

    </div>